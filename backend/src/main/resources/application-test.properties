app.production=false

# Datasource

spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/diploma?createDatabaseIfNotExist=TRUE&serverTimezone=UTC&useUnicode=TRUE&characterEncoding=UTF-8
spring.datasource.username= root
spring.datasource.password= mysqlpassword
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# logging.file.name=logs.log

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQL5InnoDBDialect

# Security

security.tokenSecret=Secret
security.tokenExpirationInMs=7200000
security.tokenIssuer=minilooth.by

spring.security.filter.order=10

# Responses

server.error.include-message=always
server.error.include-binding-errors=always

# Requests

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=miniloothstyle@gmail.com
spring.mail.password=ozggxejsrlnvkknh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.cloud.config.enabled=false
